# Versioned documentation excerpts

This directory stores checked-in Qiskit documentation slices grouped by target SDK version. Each JSON file is intentionally compact and provides retrieval-friendly summaries for one release family so offline RAG prompts can cite version-aware guidance without network access.
