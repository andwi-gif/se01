schema_version = "1.0"
output_root = "artifacts/runs"

[run]
name = "offline-qiskit-smoke"
sdk = "qiskit"
versions = ["1.0"]
modes = ["vanilla", "rag_docs"]
max_tasks = 3
notes = "Minimal offline smoke config for validating checked-in sample inputs."

[data]
task_file = "sample_data/tasks/qiskit_pilot_tasks.json"
docs_path = "sample_data/docs"
model_response_file = "sample_data/model_responses/qiskit_saved_responses.json"
