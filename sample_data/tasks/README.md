# Sample Qiskit tasks

`qiskit_pilot_tasks.json` contains 12 curated pilot tasks for the offline MVP. The tasks are intentionally small, reviewable, and cover the Qiskit-only surfaces called out in the planning docs: primitives, transpilation, circuit library usage, control flow, observables, and QASM round-trips.
