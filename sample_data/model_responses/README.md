# Offline model response fixtures

`qiskit_saved_responses.json` contains deterministic saved responses for each pilot task and each MVP generation mode (`vanilla`, `rag_docs`, `rewrite`). The fixtures are intentionally simple and exist so the future generation pipeline can run without external API keys.
